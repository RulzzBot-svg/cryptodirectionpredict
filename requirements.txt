ccxt>=4.0.0
sqlalchemy>=2.0.0
pandas>=2.0.0
ta>=0.11.0
python-dotenv>=1.0.0
# asyncio is part of the Python standard library (3.4+) and does not need to be installed
# ccxt.pro WebSocket support is included in the ccxt package (import ccxt.pro)
