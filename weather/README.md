# Moved

The weather Kalshi paper bot now lives in its own repo:

**https://github.com/RulzzBot-svg/weatherpredict**

This folder is only a pointer so the old crypto-repo path does not keep a
second copy. Open that repo (or a Cursor agent attached to it) for the
multi-city paper bot.
